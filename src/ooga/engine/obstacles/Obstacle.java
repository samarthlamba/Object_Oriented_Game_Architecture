package ooga.engine.obstacles;

import javafx.scene.Node;

public abstract class Obstacle extends Node implements Collideable {

  public Obstacle(int sceneWidth,int sceneHeight, double initialX, double initialY) {
  }
}
